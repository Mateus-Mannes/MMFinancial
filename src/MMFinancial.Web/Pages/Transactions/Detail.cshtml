@page "{date?}"
@model MMFinancial.Web.Pages.Transactions.DetailModel
@{
}

<abp-card>
    <abp-card-header>
        <h3>Traction Details</h3>
    </abp-card-header>
    <abp-card-body>
        <abp-row>
            <abp-column>
               <p><font size="4"><b>Trasaction Date:</b> @Model.Transactions.First()._DateTime.ToString("dd/MM/yyyy")</font></p>
                <p><font size="4"><b>Creator: </b>@Model._User.UserName</font></p>
                <p><font size="4"><b>Uploaded in:</b> @Model.Transactions.First().CreationTime.ToString("dd/MM/yyyy HH:mm:ss")</font></p>
            </abp-column>
        </abp-row>
        <br />
        <abp-row>
            <abp-column>
                <abp-table>
                    <thead>
                        <tr>
                            <th scope="Column">Bank</th>
                            <th scope="Column">Account</th>
                            <th scope="Column">Agency</th>
                             <th scope="Column">Bank</th>
                            <th scope="Column">Account</th>
                            <th scope="Column">Agency</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach( var Transaction in Model.Transactions){
                            <tr>
                                <td>@Transaction.BankFrom</td>
                                <td>@Transaction.AccountFrom</td>
                                <td>@Transaction.AgencyFrom</td>
                                <td>@Transaction.BankTo</td>
                                <td>@Transaction.AccounTo</td>
                                <td>@Transaction.AgencyTo</td>
                            </tr>
                            }
                    </tbody>
                </abp-table>
            </abp-column>
        </abp-row>

    </abp-card-body>
</abp-card>